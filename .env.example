# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.develop

# Database (локально — SQLite; Docker — через .env.docker.example)
# DATABASE_URL=postgres://distage:password@db:5432/distage_db

# Email — Gmail SMTP (App Password)
EMAIL_HOST_USER=your-gmail@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-16-chars
DEFAULT_FROM_EMAIL=your-gmail@gmail.com
LEAD_NOTIFICATION_EMAIL=your-gmail@gmail.com

# DigitalOcean Docker — див. .env.docker.example
